# BIBDK Cancel Order

Cancel an order which has been placed with the submitOrder mutation.

It is possible to cancel an order at any agency the user is registered.

<InlineGraphiQL
  query={`mutation cancelOrder($orderId: String!, $agencyId: String!) {
  cancelOrder(orderId: $orderId, agencyId: $agencyId, dryRun: true) {
    cancelled
    error
  }
}`}
  variables={{ orderId: "78434523", agencyId: "715100" }}
/>
