/**
 * Divider component
 *
 * Renders a visual divider line for separating sections in the UI.
 */

import styles from "./Divider.module.css";

export function Divider() {
  return <div className={styles.divider} />;
}
